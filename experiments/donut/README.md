# Vietnamese Receipt KIE — Donut (Optional)

Code Donut fine-tuning được giữ riêng trong thư mục này.

## Mục đích

Đây là **Experiment 4** — so sánh hướng OCR-free end-to-end (Donut) với hướng OCR-based modular pipeline (Exp 1-3).

## Dữ liệu đã có

- `data/mc-ocr/donut_format/` — MC-OCR đã convert sang Donut format
- `data/sroie/donut_format/` — SROIE đã convert sang Donut format

## Cách chạy (nếu cần)

Xem conversation log hoặc notebook Kaggle từ các lần train trước.

## Kết quả dự kiến

Donut fine-tuning cần nhiều dữ liệu và tài nguyên GPU hơn so với OCR-based pipeline.
Dùng để phân tích so sánh trong báo cáo.
